name: Bug Report
description: Report a bug or issue with ZenMux
title: "[Bug]: "
labels: ["bug", "needs-triage"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for reporting a bug! Please fill out the information below to help us resolve the issue quickly.

  - type: dropdown
    id: category
    attributes:
      label: Category
      description: Which area of ZenMux is affected?
      options:
        - API (request handling, response, errors)
        - Routing (model routing, load balancing, fallback)
        - Dashboard (web console, settings, analytics)
        - SDK (client libraries, code generation)
        - Billing (pricing, usage, invoices)
        - Documentation (docs site content, examples)
        - Other
    validations:
      required: true

  - type: textarea
    id: description
    attributes:
      label: Describe the bug
      description: A clear description of what the bug is
      placeholder: |
        When I call the API with model X, I get an unexpected error...
        The dashboard shows incorrect usage data...
    validations:
      required: true

  - type: textarea
    id: reproduction
    attributes:
      label: Steps to reproduce
      description: Detailed steps to reproduce the behavior
      placeholder: |
        1. Send a request to /v1/chat/completions with model "openai/gpt-4o"
        2. Set temperature to 0.7
        3. Observe the response
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: Expected behavior
      description: What you expected to happen
    validations:
      required: true

  - type: textarea
    id: actual
    attributes:
      label: Actual behavior
      description: What actually happened (include error messages, status codes, or response bodies if applicable)
    validations:
      required: true

  - type: dropdown
    id: severity
    attributes:
      label: Severity
      description: How severely does this affect your usage?
      options:
        - Critical - Service is down or data loss
        - High - Major feature is broken, no workaround
        - Medium - Feature is broken but has a workaround
        - Low - Minor issue, cosmetic or edge case
    validations:
      required: true

  - type: textarea
    id: environment
    attributes:
      label: Environment
      description: Help us understand your setup
      placeholder: |
        - API Base URL: https://api.zenmux.ai
        - SDK: Python / Node.js / Go / cURL
        - SDK Version: x.x.x
        - Model: openai/gpt-4o
      render: markdown

  - type: textarea
    id: screenshots
    attributes:
      label: Screenshots / Logs
      description: Add screenshots, error logs, or request/response examples
      placeholder: Drag and drop images here, or paste error logs

  - type: textarea
    id: additional
    attributes:
      label: Additional context
      description: Any other context about the problem
      placeholder: |
        - This started happening after...
        - Only occurs when...
        - Related to issue #123

  - type: checkboxes
    id: checklist
    attributes:
      label: Pre-submission checklist
      options:
        - label: I have searched existing issues to avoid duplicates
          required: true
